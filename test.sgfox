class Persona

    func init(nombre, edad)
        this.nombre = nombre
        this.edad = edad
    end
end

class Gerente as Persona
    func init(nombre, edad, departamento)
        dodefault(nombre, edad)
        this.departamento = departamento
    end
end

local pepe = new Gerente("Gonzalo", 35, "Ventas")
info(pepe.departamento)